Компилировал строкой, которая лежит в compile.sh(использовался чей-то BigInt)
По дефолту в hw6.cpp стоит "//#define FAST", что означает, что используются рекурсивные реализации функций, в этом случае функция Аккермана не запускается(очень долго работает). При раскомментировании включаются нативные реализации, и запускается тест функции Аккермана.
Реализацию функций из 6 ДЗ можно найти в "primitives.hpp"(сверху нативная реализация, снизу рекурсивная)
Реализацию функций из 7 ДЗ можно найти в "ackermaann.hpp"
Также в "functions.hpp" - реализации самих Z, N, U, S, R, M.
Вроде тесты подобрал, чтобы выполнялись более-менее не долго. Тесты с рекурсивными функциями работали 1,5 минуты, с функцией аккермана - минуту.
